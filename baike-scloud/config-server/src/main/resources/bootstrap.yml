server:
  port: 9401


#远程配置
spring:
    application:
        name: config-server
    cloud:
        config:
            label: master
            server:
                git:
                    password:
                    searchPaths: config-pro
                    uri:  https://github.com/tiandaguang/cloudConfig
                    username:
