my:
  name: tianda
  age: 28
  number: ${random.int}
  uuid: ${random.uuid}
  max:  ${random.int(10)}
  value: ${random.value}
  greeting: hi i,m ${my.name}}

spring:
  zipkin:
    base-url: http://localhost:9411
  boot:
    admin:
      client:
        url: http://localhost:9600
